# Production (going live) — used by build:prod, generate:prod, deploy
NUXT_PUBLIC_API_BASE=https://api.svrve.com
NUXT_PUBLIC_SITE_URL=https://svrve.com

# Local development — copy to .env.local and use with dev:local / build:local
# NUXT_PUBLIC_API_BASE=http://localhost:9090
# NUXT_PUBLIC_SITE_URL=http://localhost:3000
